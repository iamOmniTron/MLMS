<!DOCTYPE html>
<html lang="en">
<%-include("../partials/head")%>
<body>
    <%-include("../partials/navbar")%>
    <div class="container mt-5 mb-5">
        <div class="card" id="product-img">
            <div class="row g-0">
                <div class="col-md-6 border-end">
                    <div class="d-flex flex-column justify-content-center">
                        <div class="main_image">
                        <img src="http://localhost:8000/../../<%=product.imageUrl%>" class="img-fluid" alt="thumbnail"/>
                        </div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="p-3 right-side">
                        <div class="d-flex justify-content-between align-items-center">
                            <h3><%product.name%></h3>
                        </div>
                        <div class="mt-2 pr-3 content">
                            <p><%=product.description%></p>
                        </div>
                        <h3>₦<%=product.price%></h3>
                        <div class="buttons d-flex flex-row mt-5 gap-4">
                            <a href="/products" class="btn btn-warning text-center">Shop</a>
                            <form action="/cart/add/<%=product.id%>" method="post" id="add-to-cart">
                                <button class="btn btn-warning" type="submit">Add To Cart</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <%-include("../partials/foot")%>
</body>
</html>
